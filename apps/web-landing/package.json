{
  "name": "@sidekick/web-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css dist/ && bun build src/main.js --outfile dist/main.js --target=browser --minify",
    "serve": "bun run build && bun run serve:dist",
    "serve:dist": "bun run server.js"
  }
}
