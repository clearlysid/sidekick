{
  "name": "sidekick-monorepo",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "web:serve": "bun run --cwd apps/web serve",
    "web:build": "bun run --cwd apps/web build",
    "android:wear:build": "cd apps/android && ./gradlew :wear:assembleDebug"
  }
}
