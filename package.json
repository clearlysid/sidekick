{
  "name": "sidekick-monorepo",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "web:serve": "bun run --cwd web serve",
    "web:build": "bun run --cwd web build",
    "android:wear:build": "cd android && ./gradlew :wear:assembleDebug"
  }
}
